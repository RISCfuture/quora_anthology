source 'https://rubygems.org'
git_source(:github) do |repo_name|
  repo_name = "#{repo_name}/#{repo_name}" unless repo_name.include?('/')
  "https://github.com/#{repo_name}.git"
end

gem 'typhoeus'
gem 'nokogiri'

gem 'json'
gem 'activesupport'
gem 'actionpack'

gem 'rubypants', github: 'jmcnevin/rubypants'

gem 'watir'
gem 'watir-scroll'
